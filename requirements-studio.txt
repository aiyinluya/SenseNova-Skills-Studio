# SenseNova Skills Studio — minimal runtime (install from repo root)
# Gradio 5.x: pass theme to gr.Blocks(), not launch()
gradio==5.14.0
huggingface_hub==0.25.2
python-dotenv>=1.0.0
httpx>=0.27.0
pandas>=2.0.0
openpyxl>=3.1.0
